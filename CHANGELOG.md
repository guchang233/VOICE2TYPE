# 更新日志

本项目所有重要变更均会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

## [0.2.0] - 2026-08-27

### 新增
- **视频配音（一键字幕识别 + TTS 配音）**
  - 选择本地视频后一键完成：提取音轨 → 云端 ASR 带时间戳转写 → 逐段 Fish Audio TTS → 按原时间轴拼接配音音轨 → 替换原声导出新视频，并同步导出同名 SRT 字幕文件
  - **默认接入阿里云百炼 Qwen ASR**（qwen3-asr-flash-filetrans）：本地分块自动上传百炼临时存储 → 异步转写 → 句级毫秒时间戳，支持最长 12 小时音频；在「设置 → API 密钥」填写阿里云百炼 Key 即用，未配置时自动回退为整段识别配置
  - 配音页可切换识别引擎（阿里云 / 跟随整段识别配置），选择持久化保存
  - 流水线采用节点化架构：提取 / 识别 / 合成 / 混流四个独立节点经有界通道衔接，识别与合成重叠执行缩短总耗时，在途数据量受通道容量约束、峰值内存只与单个分块相关
  - 云端备用通路复用「设置 → 整段识别」配置，优先 `verbose_json`、自动回退 `srt` 解析时间戳；长视频按 10 分钟分块上传以适配各厂商限额
  - 配音音色与「语音合成」页一致，音色库在配音节点内可直接切换；每段配音经 ffmpeg atempo 变时基精确拉伸/压缩到原分段时长（不变调），音画一次直出
  - ffmpeg 自动就绪：优先使用系统 PATH 中的 ffmpeg，缺失时自动下载内置引擎到模型目录（多镜像回退 + 大小校验）
  - 处理过程五节点进度链展示与实时字幕预览（增量推送），支持取消；单段合成失败仅跳过留静音，不中断任务
  - 输出优先视频流直拷贝混流（秒级完成），编码不兼容时自动降级重编码

### 变更
- 更新下载器抽出通用 `download_file_any` 接口，供 zip 资源包复用（保留原 EXE 魔数校验语义）

### 修复
- **视频配音 TTS 全部失败（Fish Audio `Reference not found`）**
  - 根因一：客户端在未选择音色时强制填充硬编码的系统音色 ID，该 ID 已在 Fish Audio 服务端失效。现改为空值时**省略** `reference_id` 字段，由模型默认音色兜底（同时修复「语音合成」页）
  - 根因二：Fish Audio 流式返回的 WAV 头部 `data` 块大小字段不可靠，解码器改为以真实数据流结束为准，不再轻信头部长度
  - 兜底增强：显式选择的音色失效时自动清除 `reference_id` 回退默认音色重试；开局连续多段合成失败时快速中止并给出配置检查提示，避免产出全静音视频
  - 新增真实 API 集成测试（ignored，手动触发）覆盖上述两个场景
- **TTS API Key 已配置却提示未配置（偶发）**：配置为异步加载回填，回填完成前 `collectTtsConfig` 读到空输入框会把内存中的 Key 覆盖为空串并被防抖保存持久化。新增 `ensureTtsUiLoaded` 统一入口（进页/合成/开音色库前先确保配置已回填），且收集配置时未回填状态下不再用空控件值覆盖已有 Key；合成前也先等待配置就绪再校验，消除竞态误报

### 优化
- **官方音色库加载体验**：无 API Key / 配置加载失败 / 请求失败时直接在音色库模态框内展示明确提示（此前无 Key 时提示只写在语音合成页状态栏，从配音节点打开时永远停留在「加载中…」）；失败时也同步刷新分页信息；统一 `escapeHtml` 为单一纯字符串实现（含引号转义），清理历史拼接遗留的重复定义
- **配音音画同步重构（一次直出）**：弃用不可靠的「语速重试逼近」方案，改为确定性精确贴合——合成前按文本粗估语速预调（限幅 0.8–1.5×），合成后去首尾静音对齐人声起点，再用 ffmpeg `atempo` 变时基（不变调）把每段精确拉伸/压缩到原分段时长，每段都从原起始时间开口且时长完全贴合，零截断、零级联后移；atempo 异常时才回退截尾；阿里 ASR 分段边界按词级时间戳向内收紧去除句级衬垫；导出结果展示精确贴合/截尾段数统计

### 性能
- 转写预览改为增量推送与增量渲染：后端只发新增分段，前端追加 DOM 并裁剪旧节点，长视频不再随进度卡顿
- 音频上传按需从磁盘读取，回退重试时重新读取，峰值只持有一份音频数据

## [0.1.9] - 2026-08-16

### 新增
- **多字幕场景（多窗口）**
  - 可创建多个独立字幕窗口，每个窗口独立配置样式、位置、置顶/穿透、OBS 兼容模式
  - 窗口位置拖动即记忆，关闭窗口自动停用场景
- **同声传译**
  - 接入 OpenAI 兼容 LLM 接口（SiliconFlow / DeepSeek / Groq 等），实时把识别原文翻译成目标语言
  - 增量句段翻译 + 中间结果同声预览，译文落地即上屏
- **系统音频采集**：支持捕获电脑正在播放的声音（WASAPI loopback），无需额外设备
- **双音源同传模式**：系统声音 → 识别翻译为译文，同时麦克风原声作为副字幕
- **实时转录与会话纪要**：会话定稿句段自动记录（含说话人/时间/译文），可导出 TXT / SRT / Markdown
- **字幕自动换行滚动**：长文本按行宽自动切行，新行从底部出现、顶部行滚出
- **字幕窗口高度自适应内容**：容纳不下时窗口自动变大，不遮文字
- **说话人自动分轮**：按停顿自动标注说话人 1/2/…，适合会议纪要

### 变更
- 字幕窗口支持配置锚点（窗口内水平/垂直位置）与卡片最大宽度
- 字幕设置面板重构为分组卡片布局
- 移除文字边框（边框颜色/宽度/圆角）相关设置
- 字幕开关热键改为系统级全局快捷键，避免触发浏览器快捷键
- 翻译引擎抽象为可插拔接口，便于后续接入其它翻译服务

### 修复
- 修复翻译结果非实时上屏（译文落地即推送上屏，不依赖下一帧 ASR）
- 修复快速切换字幕导致的卡死与内存溢出（启停串行化 + 帧事件合并发射）
- 修复 OBS 窗口采集画面静止与透明黑底问题
- 修复语音合成页缩窗时布局重叠
- 修复字幕行数计算精度问题（改用 round 而非 ceil）

### 性能
- 字幕帧事件合并发射（固定节拍 + 脏标记），避免高频事件击穿 WebView 消息队列
- 转录与译文改为增量推送并设置上限，内存占用有界
- 字幕会话结束销毁动态窗口，释放 WebView 内存

## [0.1.8] - 2026-08-12

### 新增
- **Fish Audio 文本转语音（TTS）功能**
  - 调用 Fish Audio 免费 S2.1 PRO FREE 模型进行文本转语音
  - 支持连接官方音色库，可搜索、分页浏览、按语言/排序/自建筛选
  - 支持调整合成参数：语速、音量、温度、top_p、chunk_length、mp3_bitrate、输出格式
  - 支持生成语音、播放、下载导出
  - 设置面板支持展开/收缩，参数实时保存
  - 自动检测并使用 Windows 系统代理（含环境变量与注册表）

### 修复
- **后端安全修复**
  - 修复 Windows 注册表 ProxyServer 读取越界（UB），增加返回值检查与缓冲区长度上限
  - `tts_export` 增加源路径校验，防止路径遍历攻击
  - SOCKS 代理解析修正，支持 `socks=` 前缀并正确映射为 `socks5://`
  - 代理 URL 日志脱敏，移除可能包含的凭据信息
- **前端安全修复**
  - 新增 `escapeAttr` 函数，修复 `escapeHtml` 在属性上下文未转义引号导致的 XSS
  - `showConfirmDialog` 对 title/message/confirmText 进行 HTML 转义

### 变更
- 将「试听合成」按钮改为「生成」按钮，移除自动播放逻辑，由用户手动控制播放
- 每次生成使用 UUID 唯一文件名，解决再次生成时播放器仍播放上一条音频的问题
- 生成按钮增加重入防护：文本改动时弹窗确认，生成期间隐藏播放器并禁用按钮
- 阻塞文件 IO 改用 `tokio::fs`，避免阻塞 tokio 运行时
- preview 文件清理改为忽略扩展名，防止跨格式残留
- `docs/` 目录加入 .gitignore

## [0.1.7] - 2026-08-10

### 变更
- 版本发布与构建配置调整
