name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Tag name for the release (e.g. v0.1.2)"
        required: true
        default: v0.1.2

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  build-release:
    name: Build & Release (Windows)
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: x86_64-pc-windows-msvc

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}

      - name: Determine tag name
        id: tag
        run: |
          if [ -n "${{ github.event.inputs.tag_name }}" ]; then
            echo "name=${{ github.event.inputs.tag_name }}" >> $GITHUB_OUTPUT
          else
            echo "name=${GITHUB_REF_NAME}" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - name: Build release binary
        working-directory: src-tauri
        run: cargo build --release --target x86_64-pc-windows-msvc

      - name: Rename binary
        id: binary
        shell: bash
        run: |
          # 提取版本号（去掉 'v' 前缀，例如 v0.1.2 -> 0.1.2）
          VERSION="${{ steps.tag.outputs.name }}"
          VERSION="${VERSION#v}"
          # 源文件路径（cargo 输出的 exe 名称来自 Cargo.toml 的 [package].name）
          SRC="src-tauri/target/x86_64-pc-windows-msvc/release/voice2type.exe"
          # 目标文件名带版本号和架构
          DEST="Voice2Type_v${VERSION}_x86_64.exe"
          cp "$SRC" "$DEST"
          echo "path=$DEST" >> $GITHUB_OUTPUT
          echo "Created release binary: $DEST"
          ls -lh "$DEST"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.name }}
          name: "Voice2Type ${{ steps.tag.outputs.name }}"
          body: |
            Voice2Type 桌面版自动构建发布。

            ## 安装说明

            下载下方的 `Voice2Type_v*.exe` 文件即可直接运行，无需安装。

            **系统要求：**
            - Windows 10/11 (x86_64)
            - WebView2 运行时（Windows 11 已内置，Windows 10 可能需要安装）

            ## 特性

            - 基于 Tauri 2.0 架构，蓝色主题 UI
            - 支持流式语音识别与整段识别
            - 实时字幕功能，可自定义样式
            - 支持 OBS 捕捉
          draft: false
          prerelease: false
          generate_release_notes: true
          files: ${{ steps.binary.outputs.path }}

      - name: Update update.json for jsDelivr CDN
        run: |
          VERSION="${{ steps.tag.outputs.name }}"
          # 获取刚创建的 release 信息（发布时间、body）
          RELEASE_JSON=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases/latest")
          PUBLISHED_AT=$(echo "$RELEASE_JSON" | python3 -c "import sys,json; print(json.load(sys.stdin).get('published_at',''))" 2>/dev/null || echo "")
          BODY=$(echo "$RELEASE_JSON" | python3 -c "import sys,json; print(json.load(sys.stdin).get('body',''))" 2>/dev/null || echo "")

          # 生成 update.json
          cat > update.json << EOF
          {
            "tag_name": "${VERSION}",
            "name": "Voice2Type ${VERSION}",
            "body": "$(echo "$BODY" | sed 's/\\/\\\\/g; s/"/\\"/g; s/$/\\n/' | tr -d '\n' | sed 's/\\n$//')",
            "prerelease": false,
            "draft": false,
            "published_at": "${PUBLISHED_AT}",
            "assets": [
              {
                "name": "Voice2Type_v${VERSION#v}_x86_64.exe",
                "browser_download_url": "https://github.com/guchang233/VOICE2TYPE/releases/download/${VERSION}/Voice2Type_v${VERSION#v}_x86_64.exe"
              }
            ]
          }
          EOF

          # 提交到仓库（触发 jsDelivr CDN 缓存更新）
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add update.json
          git commit -m "chore: update update.json for ${VERSION}" || echo "No changes to commit"
          git push
